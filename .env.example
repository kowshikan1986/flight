# Django Configuration
# IMPORTANT: Keep your secret key secure and never commit it to version control
# Generate a new secret key using: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
DJANGO_SECRET_KEY=change-me
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=*
DJANGO_CSRF_TRUSTED_ORIGINS=

# Database Configuration
# WARNING: Update these credentials with your actual database credentials
# Do NOT use the example credentials in production
DB_NAME=jaffna
DB_USER=admin
DB_PASSWORD=Admin123
DB_HOST=ec2-13-235-113-205.ap-south-1.compute.amazonaws.com
DB_PORT=5432

# Email Configuration
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=localhost
EMAIL_PORT=25
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=false
DEFAULT_FROM_EMAIL=noreply@travel-booking.local

# Stripe Configuration
STRIPE_MODE=test
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=

# CORS Configuration
CORS_ALLOWED_ORIGINS=

# Security Configuration
SESSION_COOKIE_SECURE=false
CSRF_COOKIE_SECURE=false

# Celery Configuration
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/1

# Feature Flags
FEATURE_HOTELS_ENABLED=true
